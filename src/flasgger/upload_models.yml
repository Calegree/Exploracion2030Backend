get:
  tags:
    - upload
  summary: Listar modelos subidos
  responses:
    "200":
      description: Lista de modelos
      schema:
        type: object
        properties:
          models:
            type: array
            items:
              type: string